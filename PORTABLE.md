# FrameRaw Portable

Keep `frameraw.exe` and `frameraw-backend.exe` in the same directory.
Launch `frameraw.exe`; no installation is required.

Windows 10 may require Microsoft Edge WebView2 Runtime. Windows 11 normally includes it.
